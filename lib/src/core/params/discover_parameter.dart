import 'base_parameter.dart';
class DiscoverParameter  {
  int personId;
  //DiscoverParameter({apiKey, page, pageSize, required this.personId}) : super(apiKey: apiKey, page: page, pageSize: pageSize);
  DiscoverParameter({required this.personId});
}